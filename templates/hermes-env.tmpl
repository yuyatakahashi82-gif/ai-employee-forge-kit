# {{OWNER_NAME}} の AI社員 — ~/.hermes/.env へ追記する値（gateway が読むのはこちらの .env）
# ★Discord 許可ユーザーは必ず「数値の Discord User ID」。username は解決できず未認可のままになる。
#   ID は gateway.log の「Unauthorized user: <ID>」から取得できる。
DISCORD_ALLOWED_USERS={{OWNER_DISCORD_USER_ID}}
